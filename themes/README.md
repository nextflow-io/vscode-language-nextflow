# Seqera Dark theme

Bundled color theme for the Nextflow VS Code extension.

## Preview locally

1. Open this repository in VS Code or Cursor.
2. Press `F5` to launch the Extension Development Host.
3. Open the Command Palette and run **Preferences: Color Theme**.
4. Select **Seqera Dark**.

Webviews and the DAG preview inherit `--vscode-*` CSS variables from the active theme automatically.
